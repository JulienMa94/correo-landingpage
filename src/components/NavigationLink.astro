---
interface Props {
	text: string
	href: string;
    target?: string;
    ariaLabel?: string;
}

const { text, href, target, ariaLabel } = Astro.props;
---

<a href={href} target={target} aria-label={ariaLabel} class="ml-3 font-medium">
    {text}
</a>

<style>
    a {
        color: #4a5568
    }

    a:hover {
        text-decoration: underline;
        color: black
    }
</style>