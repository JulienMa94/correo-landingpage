---
import Download from "./Download.astro";
import SectionHeadline from "../components/SectionHeadline.astro";
import correoAppImg from "../assets/image 2.png"
import { Image } from 'astro:assets';

---

<section id="#welcome_section" class="welcome_section z-0">
    <div class="flex flex-col justify-center align-center text-center mt-4">

        <div class="mt-4 mb-4">
            <h1 class="text-4xl font-bold">
                CorreoMQTT
            </h1>
        </div>

        <div class="flex justify-center">
            <p class="w-4/6 mb-4 mt-4">
                A modern graphical MQTT client using the HiveMQ client library.
                MQTT is a messaging protocol widely used in the IoT world.
                CorreoMQTT provides a UI to send and receive messages. It is able to handle multiple connections at the same time.
            </p>
        </div>

        <div class="mb-4">
            <Download />
        </div>

        <div class="flex justify-center z-0">
            <Image src={correoAppImg} alt="Correo App" class="correoAppImg"/>
        </div>
    </div>
</section>

<style>
    .welcome_section {
        position: relative;
        height: 500px;
        z-index: 2;
    }

    .correoAppImg {
        position: absolute;
        z-index: -1;
    }
</style>